`use strict`;

for (let i = 0; i < 24; i++) {
    if (i >= 8 && i <= 22) {
        console.log(`Son las ${i}:00 horas CUCÚ`);
    } else {
        console.log(`Son las ${i}:00 horas`);
    }
}

/* 
for (let i = 0; i < 24; i++) {
  if (i >= 8 && i <= 22) {
      console.log(`Son las ${i}:00 CUCÚ!`);
    } else {
      console.log(`Son las ${i}:00`);
    }
   */